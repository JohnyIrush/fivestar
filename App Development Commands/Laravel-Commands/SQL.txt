--INNER JOIN
SELECT * 
FROM attendances 
INNER JOIN students ON attendances.student_id=students.id 
INNER JOIN users ON students.user_id=users.id 
INNER JOIN sections ON students.section_id=sections.id 
INNER JOIN streams ON sections.stream_id=streams.id 
INNER JOIN levels ON sections.level_id=levels.id;


